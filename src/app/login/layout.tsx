import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Log in · Mini Z and Me",
};

export default function LoginLayout({ children }: { children: React.ReactNode }) {
  return children;
}
